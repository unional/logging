{
  "name": "logging",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "build": "tsc -b",
    "build:watch": "tsc -b -w",
    "coverage": "yarn jest --coverage",
    "test": "yarn jest",
    "verify": "yarn build && yarn coverage",
    "watch": "yarn jest --watch"
  },
  "devDependencies": {
    "@types/jest": "24.0.11",
    "@unional/devpkg-node": "^1.2.5",
    "jest": "^24.7.1",
    "jest-junit": "^6.3.0",
    "jest-watch-toggle-config": "^1.0.2",
    "lerna": "^3.13.3",
    "param-case": "^3.0.3",
    "pascal-case": "^2.0.1",
    "source-map-loader": "^0.2.4",
    "ts-loader": "^5.4.3",
    "uglifyjs-webpack-plugin": "^2.1.2",
    "webpack": "^4.30.0",
    "webpack-cli": "^3.3.1"
  }
}
